[defaults]
# Keep inventory resolution relative to the ansible directory.
inventory = inventory/hosts.yml
# Hosts are short-lived lab instances; host keys are not persisted in CI.
host_key_checking = False
# Avoid creating retry files in the repository workspace.
retry_files_enabled = False